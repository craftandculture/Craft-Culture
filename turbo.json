{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["OPENAI_API_KEY", "ANTHROPIC_API_KEY", "VERCEL_ENV", "NEXT_PUBLIC_VERCEL_ENV", "ZOHO_CLIENT_ID", "ZOHO_CLIENT_SECRET", "ZOHO_REFRESH_TOKEN", "ZOHO_ORGANIZATION_ID", "ZOHO_REGION", "WMS_DEVICE_TOKEN", "WMS_DEVICE_USER_EMAIL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    }
  }
}
