{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["OPENAI_API_KEY", "VERCEL_ENV"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    }
  }
}
