/**
 * What a condition report costs, per case
 *
 * Stated in one place because it appears in three: the button a member
 * presses, the request that is written, and the invoice that follows. The
 * published rate card carries its own figure for photography and reporting —
 * if the two ever disagree, this is the one a member will have seen.
 */
const CONDITION_REPORT_FEE_AED = 100;

export default CONDITION_REPORT_FEE_AED;
