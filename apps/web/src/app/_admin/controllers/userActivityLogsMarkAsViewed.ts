// Imports temporarily removed - will be restored after database migration

/**
 * Mark all activities as viewed for the current user
 *
 * Note: Temporarily disabled - requires database migration to add lastViewedActivityAt column
 */
const userActivityLogsMarkAsViewed = async () => {
  // Temporarily disabled until database migration is complete
  return { success: true };
};

export default userActivityLogsMarkAsViewed;
